{
    "name": "@aurum/devtools-extension",
    "version": "0.10.0",
    "private": true,
    "description": "Chromium DevTools extension for inspecting Aurum data flow",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.build.json && node scripts/copy-static.mjs && vite build --config vite.config.ts",
        "test": "tsc -p tsconfig.test.json && vitest run && npm run test:panel",
        "test:panel": "npm run build && node scripts/test-panel-scroll.mjs",
        "clean": "node scripts/clean.mjs"
    },
    "dependencies": {
        "@aurum/html": "^0.10.0",
        "@aurum/streams": "^0.10.0"
    },
    "devDependencies": {
        "playwright": "^1.62.1",
        "vitest": "^4.0.16"
    },
    "license": "MIT"
}
