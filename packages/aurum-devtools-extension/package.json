{
    "name": "@aurum/devtools-extension",
    "version": "0.1.0",
    "private": true,
    "description": "Chromium DevTools extension for inspecting Aurum data flow",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.build.json && node scripts/copy-static.mjs",
        "test": "tsc -p tsconfig.test.json && vitest run",
        "clean": "node scripts/clean.mjs"
    },
    "devDependencies": {
        "vitest": "^4.0.16"
    },
    "license": "MIT"
}
